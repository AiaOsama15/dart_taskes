List a = [2, 3, 4, 5];

void main() {
  expanded();

  //add num into arr
}

expanded() {
  List? expandedL ist;
  var cap = (a.length) * 2;
  for (int i = 0; i < a.length; i++) {
    expandedList = a[i];
    print(expandedList);
  }

  // add() {
  //   expandedList![a.length + 1] = 4;
  //   print(expandedList);
  // }
}
